# 1. Create project folder
mkdir AhmadBlog
cd AhmadBlog

# 2. Create all folders
mkdir -p models routes templates static/css static/js static/uploads data utils

# 3. Create all files (copy code above)

# 4. Install dependencies
pip install -r requirements.txt

# 5. Run the app
python run.py

# 6. Open in browser
# http://localhost:5000
